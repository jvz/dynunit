
# Add DPSLicense for when we are running just DAS and want to resolve
# a session scoped component with a request created by
# ServletTestUtils.createDynamoHttpServletRequestForSession.
#
# Since DAS only adds DAFLicense, and we have no DAFLicense, we'll
# use DASLicense, instead
licenseChecklist=/DPSLicense